{% set name = "randomgen" %}
{% set version = "v1.16.4" %}
{% set sha = "a7e9fa405f41545f590f78ed088d1c997b28ba1800eb3b23e5aa63f082a1b297" %}
package:
    name: {{ name|lower }}
    version:  {{ version }}

source:
    version: {{ version }}
    url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version[1:] }}.tar.gz
    sha256: {{ sha }}

build:
    number: 0
    script: {{ PYTHON }} -m pip install --no-deps --ignore-installed .

requirements:
    build:
        - {{ compiler('c') }}
    host:
        - python
        - pip
        - cython
        - numpy
    run:
        - python
        - {{ pin_compatible('numpy') }}

test:
    requires:
        - pytest
        - nose

about:
    home: 'https://github.com/bashtage/randomgen'
    license: NSCA
    license_file: LICENSE.md
    summary: Replacement random number generator for NumPy
